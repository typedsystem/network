[project]
name = "network"
version = "0.0.0"
description = "The network library from typedsystem"
readme = "README.md"
requires-python = ">=3.9"
license = {text = "MIT"}
authors = [
    {"name" = "Yuri Ximenes", "email" = "ximenesyuri@gmx.ie"},
]
dependencies = [
]

[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["."]
include = ["network*"]
